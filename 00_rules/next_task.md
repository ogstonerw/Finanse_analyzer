# Следующая задача

Продолжить подготовку полного demo-flow MVP для сдачи диплома.

Нужно:

- запустить локальную PostgreSQL БД и проверить auto-migrate при старте backend;
- создать первого пользователя через `POST /api/v1/auth/register`;
- пройти frontend demo-flow: вход, dashboard, активы, новости/события, генерация прогноза, история прогнозов;
- проверить `AI_MODE=prepare` и `AI_MODE=openai` при наличии API-ключа;
- подготовить markdown-текст для разделов 3.1-3.3 ВКР на основе фактической реализации;
- после завершения обновить соответствующий файл в `05_session_logs/`.
